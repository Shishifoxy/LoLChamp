{% extends 'base.html.twig' %}

{% block title %}Champ{% endblock %}

{% block body %}
    <h1>Champ</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ champ.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ champ.name }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ champ.title }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ champ.description }}</td>
            </tr>
            <tr>
                <th>Ressource</th>
                <td>{{ champ.ressource }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('champ_index') }}">back to list</a>

    <a href="{{ path('champ_edit', {'id': champ.id}) }}">edit</a>

    {{ include('champ/_delete_form.html.twig') }}
{% endblock %}
